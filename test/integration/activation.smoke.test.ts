/**
 * Integration Test: Activation / Command Registration / TreeView
 *
 * Requires @vscode/test-electron with actual VS Code host.
 * Run with: pnpm test:integration
 */
import { describe, it } from 'vitest';

describe.skip('Activation Smoke Test (requires VS Code host)', () => {
  it('extension activates successfully', () => {});
  it('all 7 commands are registered', () => {});
  it('both TreeViews are registered', () => {});
});
